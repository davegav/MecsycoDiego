co-simulation TrafficGridExperiment

//Minimal experiment example
model path "Library/Multimodel/TrafficGridExample.xml"

information description "Minimal example of an experiment using 4 NetLogo models and using all default model parameters.
We see four NetLogo Traffic models which are connected together, but the tore isn't complete and one model let the cars escape."

simulation parameters
	stopTime=1.
	discretization=0.01  
	windowHeight=520 
	windowWidth=520
